# Copy this file to .env and adjust as needed
PORT=4000
JWT_SECRET=kisan-setu-super-secret-change-me
# DEV_MODE=true returns the OTP in the API response + logs it to the console,
# so you can demo the full flow without paying for an SMS gateway.
# Set DEV_MODE=false and wire a real provider in src/utils/otpStore.js when you have one.
DEV_MODE=true
OTP_EXPIRY_MINUTES=5
